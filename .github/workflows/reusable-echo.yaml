name: Reusable Echo
on:
  workflow_call:
    inputs:
      message:
        description: Message to echo
        required: true
        type: string
jobs:
  echo:
    runs-on: self-hosted
    steps:
      - name: Print incoming message
        run: |
          echo "Reusable says: ${{ inputs.message }}"

